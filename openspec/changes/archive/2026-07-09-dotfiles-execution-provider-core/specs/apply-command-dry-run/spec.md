# Delta for apply-command-dry-run

No requirements are changed by `dotfiles-execution-provider-core`.

Apply command wiring, `apply --yes` behavior changes, and render/report copy for canonical dotfiles base path and selected modules are deferred to the follow-up change `wire-dotfiles-apply-yes`.
